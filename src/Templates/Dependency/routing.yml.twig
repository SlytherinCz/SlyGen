{% for route in routes %}
{{ route.getName() }}:
    path: {{ route.getPath() }}
    defaults: { _controller: {{ route.getController() }} }
    methods: [{{ route.methods | join(',') }}]

{% endfor %}